# OS
.DS_Store

# Env
.env
.env.local
.env.*.local

# Node
node_modules/
.next/
dist/

# Local learning data (session transcripts/summaries) — this is the
# specific child's data, not sample/demo content, so it never gets committed.
backend/data/
